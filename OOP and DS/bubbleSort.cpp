#include <iostream>
using namespace std;


int main () {
	
	int size;
	cout << "Enter the size of array: ";
	cin >> size;
	int arr[size];
	for (int count = 0; count <size; count ++) {
		
		cout << "Enter the array element " << count << " :";
		cin >> arr[count];
	}
	
	for (int i = 0; i < size; i++) {
		
		for (int j = 0; j < size-i; ) {
			swap 
		}
	}
	
	return 0;
}
